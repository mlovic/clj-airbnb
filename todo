-/- Browser extension
-/- HTTP interface
Email notification
Users


POST should add alert
 - add to db
 - add to sched
 - add listing if not there

monitor to ensure changes make sense. 
single db connection
spec
replace printlns with logging
check db last-updated when starting up schedule. Maybe even use instead of sched. At least sched is a in-memory cache.
solve german domain

Would it be possible to have extension scrape airbnb?
